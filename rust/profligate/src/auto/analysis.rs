#[cfg(test)]
mod tests;

/// Returns the most frequently occurring character in the given string. Case-insensitive; assumes the text is ASCII.
pub fn most_frequent_char(text: &str) -> u8 {
    unimplemented!()
}
